{
  "name": "koven",
  "version": "0.0.1",
  "private": true,
  "description": "Koven — consensus-governed chat. Web client + server (Matrix-backed).",
  "workspaces": ["client", "shared", "engine"],
  "scripts": {
    "dev": "bun run --cwd client dev",
    "dev:engine": "bun run --cwd engine dev",
    "build": "bun run --cwd client build"
  }
}
